# Distinct closing portrait

The user noted the closing portrait repeated the new listening hero pose.
Reused the existing `closing-portrait-left.jpg` and corresponding WebP as the
`closing-listen` slot's new content: standing by the window, smiling, a different
pose. No face edits or new generated imagery. The hero and mobile closing image
are unchanged. Updated intrinsic height to 908 and versioned the JPG reference.

The production CMS slot is deliberately updated alongside Git defaults so the
normal preservation step retains this change and future manager uploads remain
effective. A full pre-change archive is kept at
`/var/backups/pinhas-closing-20260916/site-before.tar.gz`.

Validation: build, SEO/link checks and four design gates passed; desktop browser
inspection confirmed the standing portrait and uncropped face/kippah.
